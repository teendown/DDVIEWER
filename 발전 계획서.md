# 🎨 벡터 기반 마인드맵 그래픽 편집기 UI/UX 및 시스템 아키텍처 기획서 (Final)

## 1. 핵심 아키텍처 및 좌표계 원칙 (Core Architecture & Coordinates)
* **벡터 패스 기반 드로잉:**
  * 모든 그리기 및 그래픽 생성은 **벡터 패스** 기반으로 처리한다.
  * 선을 그을 때 시작점($X_1, Y_1$)과 끝 좌표($X_2, Y_2$)를 기반으로 패스가 생성되며, 해상도 저하 없는 연산과 수정이 보장되어야 한다.
* **객체 = 레이어 1:1 동치 원칙 (Object-based Architecture):**
  * 포토샵의 복잡한 레이어 스택 개념을 지양하고, 마인드맵 특성에 맞춘 **"1개 객체 = 1개 독립 레이어"** 구조를 엄격히 적용한다.
  * 캔버스 위의 모든 선, 도형, 텍스트, 커넥터는 각각 독립된 데이터 노드(객체)로 생성 및 관리된다.
* **2계층 좌표계 분리 원칙 (확대/축소 대응 절대 좌표 시스템):**
  * **월드 좌표계 (World Space):** 캔버스 내부의 실제 데이터가 존재하는 가상의 무한한 절대 공간이다. 모든 객체의 좌표와 제어점은 **오직 이 월드 좌표계의 절대 값**으로만 영구 저장 및 관리된다.
  * **뷰포트 좌표계 (Viewport Space):** 사용자가 화면을 통해 바라보는 실제 화면 픽셀 영역이다.
  * **줌/팬 대응 변환:** 사용자가 화면을 확대/축소(Zoom)하거나 이동(Pan)할 때, **실제 객체의 월드 좌표 데이터는 절대 변경되지 않아야 한다.** 렌더링 엔진 단에서 변환 행렬(Transformation Matrix)을 적용해 시각적 위치만 처리하며, 마우스 입력 시 현재 줌/팬 값을 역산(Inverse Matrix)하여 월드 좌표로 보정 후 반영한다.

---

## 2. 인터페이스 및 패널 구조 (UI Layout)
### ① 좌측 메뉴 (그리기 도구 및 기본 속성 제어)
* **도구 실행 구조:** 일러스트레이터의 기본 도구 선택 및 실행 메커니즘을 차용한다.
* **도구별 상세 속성 목록:**
  * **[Tool 1] 펜 / 선(Path) 및 커넥터 도구 속성:**
    * **선 색상 (Stroke Color):** HEX / RGB 컬러 값
    * **선 두께 (Stroke Weight):** px 단위 굵기 조절
    * **선 스타일 (Stroke Style):** 실선(Solid), 점선(Dashed), 파선(Dotted)
    * **화살표 속성:** 시작/끝 화살표 유무 선택 및 **화살표 크기 비율 계수(Scale Ratio)** 커스텀 조절 기능 (기본은 선 굵기 비례)
  * **[Tool 2] 도형(Shape / Node) 도구 속성:**
    * **채우기 색상 (Fill Color):** 내부 배경 색상 및 투명도(Opacity)
    * **테두리 색상 (Border/Stroke Color):** 외곽선 색상 및 두께(Border Weight)
    * **모서리 둥글기 (Corner Radius):** 모서리 곡률 값
  * **[Tool 3] 텍스트(Text) 도구 속성:**
    * **폰트 패밀리 / 크기 / 정렬:** 폰트 종류, 픽셀 사이즈, 좌·우·가운데 정렬
    * **텍스트 채우기 및 테두리:** 텍스트 자체의 면 색상(Fill) 및 외곽선(Stroke) 지정
    * **간격 조절:** **자간(Letter Spacing)** 및 **줄간격(Line Height)** 조절 필드
    * **글자별 개별 색상:** 전체 적용 외에, 드래그로 지정한 **특정 글자(Character) 단위별 컬러 커스텀** 지원
* **속성 독립성 원칙:**
  * 좌측 메뉴에서 설정하는 **도구의 기본 속성값**과, 이미 캔버스에 생성된 **각각의 개별 객체가 가진 속성값은 완전히 분리**되어 독립 관리된다.

### ② 우측 패널 (상세 속성 탭 및 객체 관리)
* **슬라이딩 속성 탭:**
  * 캔버스에서 객체를 선택하면, **우측 메뉴에서 왼쪽 방향으로 속성 탭이 슬라이드되어 펼쳐지며** 해당 객체의 세부 속성을 수정할 수 있다.
* **우측 패널 내 객체 계층(Z-index) 및 삭제 제어:**
  * **순서 변경:** 객체 목록에서 드래그 앤 드롭 또는 상하 이동 버튼을 통해 **객체의 렌더링 상하 순서(Layer Stack)**를 직관적으로 변경할 수 있다.
  * **객체 삭제:** 우측 패널 내에서 즉시 객체를 **삭제(Delete)**할 수 있는 인터페이스 제공.
* **선택 및 제어 제약:**
  * 객체의 정밀한 속성 수정 및 이동 제어는 우측 패널(또는 캔버스)에서 **객체가 명시적으로 선택된 상태**에서만 활성화된다.

---

## 3. 그룹화 및 계층 관리 (Grouping)
* **그룹 및 해제:** 생성된 개별 객체들은 사용자의 선택에 따라 **자유로운 그룹화(Group) 및 그룹 해제(Ungroup)**가 가능하다.
* **그룹 속성 제어:** 단일 객체뿐만 아니라, **그룹화된 객체 전체의 공통 속성**을 일괄적으로 변경할 수 있다.

---

## 4. 객체 인터랙션 및 패스 연장 규칙 (스냅핑 포함)
* **바운딩 박스(Bounding Box)의 역할 한정:**
  * 사각 바운딩 박스는 오직 객체의 **'선택(Selection) 및 전체 이동/속성 변경'**을 위한 시각화 UI 도구로만 제한한다.
* **스냅핑(Snapping) 기능 (ON / OFF 토글 지원):**
  * **스냅핑 토글:** 상단 UI에서 사용자가 스냅핑 기능을 자유롭게 **ON/OFF** 할 수 있다.
  * **동작 방식 (ON 시):** 마우스 커서가 기존 노드의 중심점, 테두리, 또는 선분의 끝점(Anchor)/중간 지점에 인접할 때 자석처럼 달라붙어 정밀한 결합을 유도한다.
  * **동작 방식 (OFF 시):** 스냅핑 없이 사용자 좌표 그대로 자유롭게 드로잉 및 배치가 가능하다.

---

## 5. 객체 클릭 판정(Hit Testing) 및 겹침 제어 원칙
* **투명 영역 클릭 방해 문제 해결 (스트로크 기반 판정):**
  * 선(Path) 및 커넥터 객체의 경우, 바운딩 박스의 내부 빈 공간(Fill 없음)은 클릭 판정에서 배제한다. 오직 **실제 벡터 선분(Stroke)**이나 **끝점(Anchor)** 근처에 마우스가 위치할 때만 선택 및 인터랙션이 활성화된다.
* **도형 채우기(Fill) 영역과 겹침 제어:**
  * 채우기(Fill)가 활성화된 도형 객체는 면 내부 전체가 클릭 영역(Hit Area)으로 작용하므로, 하위 객체가 가려질 수 있다. 이 경우 **우측 패널의 객체 목록 선택**을 통해 해결한다.

---

## 6. 객체 자동 연결선(커넥터) 및 동적 패스 변형 기능
* **커넥터의 독립된 속성 관리:**
  * 객체 간 자동 생성되거나 사용자가 그린 커넥터(연결선)는 도형/텍스트와 분리된 **고유의 속성 탭(선 두께, 색상, 스타일, 화살표 비율 등)**을 우측 패널에서 독립적으로 가진다.
* **중간점 클릭 및 동적 패스 변형 알고리즘:**
  * **인터랙션:** 객체 A와 B를 잇는 커넥터의 중간 영역을 마우스로 클릭하여 드래그할 경우, 해당 위치에 **동적 제어점(Anchor Point, $P_{mid}$)**이 생성된다.
  * **실시간 리렌더링:** 사용자의 마우스 이동 좌표를 기반으로 선의 경로($P_1 \rightarrow P_{mid} \rightarrow P_2$)가 실시간으로 재계산되어 꺾은선 또는 부드러운 곡선 형태로 유기적으로 다시 그려진다.
* **연결성 유지:**
  * 커넥터가 변형되더라도 시작 객체(Node A)와 끝 객체(Node B) 간의 논리적 바인딩 관계는 유지되며, 원본 객체 이동 시에도 연결 구조가 깨지지 않고 연동된다.